{
//////////////////////全局設置Global Setting//////////////////////
"out_root": "E:/RAY/Unclassified",                  //根目錄，什麼路徑都沒有定義的時候放在這裡。
"default_path_template": "E:/RAY/{YYYY}/{YYYYMM}",  //默認模板路徑

//////////////////////文件分类规则Rule Item//////////////////////
"items":
  [

  
    {
      "key": "【群组】分类", //用於分组与显示,`【组名】条目名`。Display label used by the UI. Text inside the full-width brackets `【…】` becomes the group header, and the remaining text is the item label.
      "path_template": "E:/{YYYY}/{YYYYMM}/DestDir", //落地目录模板（覆盖全局 default_path_template）。可用占位 {YYYY} {MM} {YYYYMM}；Destination dir for THIS item; overrides global template.
      "dest_subdir": "subdir1/subdir2",  //该类文件应落到默认模板路径下的子目录（例如 E:/.../{YYYY}/{YYYYMM}/subdir1/subdir2）。subfolder appended to default template dir.
      "rename": "01. {YYYY}{MM}_RenameFile{ext}",  //重命名模板。{orig}=原文件名(已做安全化, 前置下划线), {ext}=原扩展名。renaming pattern.
      "present_rule": { "mode": "any" },  //完成判定。'any' 表示目录下前缀匹配到≥1个就OK；或用 {"mode":"count_at_least","n":2} 指定至少 N (=2)个。presence rule.
      "exts": [".pdf", ".xls", ".xlsx", ".csv"], //仅当文件扩展名在列表中才会被执行移动/重命名。留空或不写则不限。allowed extensions.
    }  

    {
    "key": "【群组】分类",
    "path_template": "E:/{YYYY}/{YYYYMM}/DestDir",
    "rename": "01. {YYYY}{MM}_RenameFile{ext}",
    "present_rule": { "mode": "any" },
    "exts": [".pdf", ".xls", ".xlsx", ".csv"]
  }
  
    {
    "key": "【群组】分类",
    "dest_subdir": "subdir1/subdir2",
    "rename": "01. {YYYY}{MM}_RenameFile{ext}",
    "present_rule": { "mode": "any" },
    "exts": [".pdf", ".xls", ".xlsx", ".csv"]
    }
  ]
}


